version: "3.9"

services:
  caddylander:
    build: .
    volumes:
      - caddylander_data:/var/caddy
    # Optional local test exposure using the VETO mnemonic port
    # ports:
    #   - "8386:8080"   # VETO mnemonic test port

volumes:
  caddylander_data: {}

# Example Caddyfile block for wildcard routing:
# *.example.com {
#     reverse_proxy caddylander:8080
# }
