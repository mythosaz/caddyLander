version: "3.9"

services:
  caddylander:
    image: mythosaz/caddylander:main
    volumes:
      - caddy_var:/var/caddy
    # Optional local test exposure using the VETO mnemonic port
    # ports:
    #   - "8386:8080"   # VETO mnemonic test port

volumes:
  caddy_var: {}

# Example Caddyfile block for wildcard routing:
# *.example.com {
#     reverse_proxy caddylander:8080
#     # Mount the same volume to keep content.json in sync
#     # volumes:
#     #   - caddy_var:/var/caddy
# }
